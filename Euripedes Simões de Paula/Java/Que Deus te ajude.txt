1. Analise o fluxograma abaixo:

	| Início | 
			-> | Condição A | 
				-> Sim -> | Executa a Condição A | -> | Fim do programa |
				-> Não -> | Passa para a próxima Condição |

			-> | Condição B |
				-> Sim -> | Executa a Condição B | -> | Fim do programa |
				-> Não -> | Passa para a próxima Condição |

			-> | Condição C |
				-> Sim -> | Executa a Condição C | -> | Fim do programa |
				-> Não -> | Passa para a próxima Condição |

	| Fim do Programa |

Agora veja o Cógigo abaixo:

package xxxxx;
	
public class IfElse {

	public static void main(String[] args){

		int a = 10;
		
		if (a > 0) {
			System.out.println("a é positivo");
		} else if (a < 0) {
			System.out.println("a é negativo");
		} else {
			System.out.println("a é zero");
		}

	}
}

Perguntas: 
A - O fluxograma acima está correto, em relação ao código escrito mais abaixo?
B - Me explique o que este cógigo está programado a fazer.


2. Veja o código abaixo:

package xxxxx;
	
public class SwitchCase {

	public static void main(String[] args){

		int opcao;
		
		Scanner leia = new Scanner(System.in);
		
		System.out.println(
			"Escolha uma opção: "
			"1. Primeira opção. "
			"2. Segunda opção.  "
		);
		
		opcao = leia.nextInt();
		leia.close();

		switch (opcao) {
			caso 1: {
				System.out.println("Você escolheu a primeira opção.")
				break;
			}
			caso 2: {
				System.out.println("Você escolheu a segunda opção.")
				break;
			}
			default: 
				throw new IllegalArgumentException("Escolha uma opção valida.");
		}	

	}
}

Perguntas:
A - Quais são os erros presentes no código acima?
B - Neste código, o que a estrutura Switch/case deveria fazer?

3. Veja o código abaixo:

public class EstruturaFor {

	public static void main(String[] args){

		for (int i = 0; i <= 100; i++); {
			System.out.println(i);
		}
	}
}

Pergunta:
A - Sabemos que a estrutura "for" conta com 3 parametros. Observe o código acima e diga qual é o parametro que se refere a:
	Expressão de inicialização: 
	Condição de teste:
	Contador:

B - O código acima tem um erro de sintaxe, me diga qual é.

4. Veja o código a seguir:

import java.util.Scanner

public class EstruturaWhile{

	public static void main(String[] args){
		
		int numero = -1;		

		Scanner leia = new Scanner(System.in);

		while (CONDIÇÃO) {
			
			System.out.println("\nEscolhaum número: ");
			numero = leia.nextInt();
			
			if (CONDIÇÃO) {
				System.out.println("\n\nVocê Acertou!!!");
			}			
			else {
				System.out.println("\n\nVocê errou...");
			}
			
		}
		
	}
}

A - Escreva qual deve ser a condição criada dentro dos parametros para "while" e "if".
B - Explique o que este código deverá realizar.

5. Veja o código abaixo:

import java.util.Scanner;

public class EstruturaWhile{

	public static void main(String[] args){
		
		int numero, resultado, contador = 1;		

		Scanner leia = new Scanner(System.in);

		while(CONDIÇÃO) {
			System.out.println(
				"\n\nDigite o " + contador + "º número: "
			);
			numero = leia.nextInt();
			
			resultado = numero * 3;
			System.out.println(numero + " x 3 = " + resultado);
		
			
		}

	}
}

Perguntas:
A - Está faltando algo no código. Escreva quais são as condições e linha necessárias para o código funcionar?
B - Explique o que este código deverá realizar.

6. 
A - A estrutura DO/WHILE executará um bloco de código e depois repetirá o processo caso sua condição for verdadeira. Me explique o que ele executará caso a condição seja falsa.
B - Escreva a sintaxe do DO/WHILE que seja funcional.

7. Abaixo criamos 4 vetores:

A = {1, 2, 3, 4, 5}
B = {6, 7, 8, 9, 10}
C = {1, 2, 3, 4, 5}
D = {11, 12, 13, 14}

Perguntas:
A - Quando usamos a estrutura if, e iniciamos como parametro o seguinte comando o "Arrays.equals()", explique o que acontecerá.
B - crie uma estrutura if, onde os vetores iguais não são selecionados.

8. Veja o código abaixo:

int[][] matriz = {
	{"a","b","c","d"},
	{"e","f","g","h"},
	{"i","j","l","m"}
}

Perguntas: 
Diga o que o código abaixo buscará:

A - System.out.println(matriz[1][1]);
B - System.out.println(matriz[0][2]);

9. 

public class EstruturaWhile{

	public static void main(String[] args){
		
		int[] vetorA = {1, 2, 3};
		int[] vetorB = {4, 5, 6};
		int[] vetorC = new int[3];
		 
		for (int linha = 0; linha < vetorA.length; linha++) {
			vetorC[linha] = vetorA[linha] + vetorB[linha];
		}
		 
		System.out.println("Soma dos vetores: ");
		for (int num : vetorC) {
			System.out.println(num);
		}
	}
}

Perguntas
A - Qual será o resultado exibido do código acima?
B - Explique o que o código acima deverá realizar?

10. Qual é o resultado para a soma das matrizes? (levando em consideração o que foi iniciado no código abaixo )

		int[][] matrizA = {
			{1, 2, 3},
			{4, 5, 6}
		};
		int[][] matrizB = {
			{7, 8, 9},
			{10, 11, 12}
		};
		int[][] matrizC = new int[2][3];

	