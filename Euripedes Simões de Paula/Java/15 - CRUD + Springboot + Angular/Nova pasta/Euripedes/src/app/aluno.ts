/*  Nesta classe vamos trabalhar com a implementação de nosso objeto.
    Vamos criar o nosso objeto, a fim de exportar os atributos para outras
    classes e documentos.   */
export class Aluno {

    /* Vamos criar os mesmos atributos que criamos no Back-end: */
    id!: number;
    nome!: string;
    sobrenome!: string;
    email!: string;

    /*  Agora vamos criar uma classe com o comando "ng g c lista". Depois
        siga para o documento "app.lista.html"  */
}
